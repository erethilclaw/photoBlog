{% extends 'front/base.html.twig' %}

{% block body %}
{{ form_start(mailForm) }}
                {{ form_widget(mailForm) }}
    <button type="submit" >{{ 'form.submit'|trans }}</button>
    {{ form_end(mailForm) }}
{% endblock %}

{% block title %}

{% endblock %}

